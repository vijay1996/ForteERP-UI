export interface PageStateInterface {
    activeTab?: number,
    showSidebar?: boolean,
    sidebarHeight?: number,
    application?: string,
    screen?: string,
    metadata?: any,
    loader?: boolean,
    form?: string|null
}